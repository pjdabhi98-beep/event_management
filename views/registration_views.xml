<?xml version="1.0" encoding="utf-8"?>

<odoo>
   <!-- Registration List View -->
<record id="view_event_registration_list" model="ir.ui.view">
    <field name="name">event.registration.list</field>
    <field name="model">event.registration</field>
    <field name="arch" type="xml">
        <list string="Registrations">
            <field name="name"/>
            <field name="participant_id"/>
            <field name="registration_date"/>
            <field name="state"/>
        </list>
    </field>
</record>


    <!-- Registration Form View -->
    <record id="view_event_registration_form" model="ir.ui.view">
        <field name="name">event.registration.form</field>
        <field name="model">event.registration</field>
        <field name="arch" type="xml">
            <form string="Registration">

                <header>
                    <button name="action_confirm"
                            string="Confirm"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"/>

                    <button name="action_cancel"
                            string="Cancel"
                            type="object"
                            class="btn-secondary"
                            invisible="state == 'cancelled'"/>

                    <button name="action_reset_to_draft"
                            string="Reset to Draft"
                            type="object"
                            class="btn-secondary"
                            invisible="state != 'cancelled'"/>

                    <field name="state"
                           widget="statusbar"
                           statusbar_visible="draft,confirmed,cancelled"/>
                </header>

                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="participant_id"/>
                        </group>

                        <group>
                            <field name="registration_date"/>
                            <field name="state"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Notes">
                            <field name="notes"/>
                        </page>
                    </notebook>
                </sheet>

            </form>
        </field>
    </record>


    <!-- Registration Search View -->
    <record id="view_event_registration_search" model="ir.ui.view">
        <field name="name">event.registration.search</field>
        <field name="model">event.registration</field>
        <field name="arch" type="xml">

            <search string="Search Registrations">

                <!-- Search Fields -->
                <field name="name"/>
                <field name="participant_id"/>
                <field name="state"/>
                <field name="registration_date"/>


                <!-- Filters -->
                <filter name="filter_draft"
                        string="Draft"
                        domain="[('state', '=', 'draft')]"/>

                <filter name="filter_confirmed"
                        string="Confirmed"
                        domain="[('state', '=', 'confirmed')]"/>

                <filter name="filter_cancelled"
                        string="Cancelled"
                        domain="[('state', '=', 'cancelled')]"/>


                <!-- Group By -->
                <group expand="0" string="Group By">

                    <filter name="group_by_state"
                            string="Status"
                            context="{'group_by': 'state'}"/>

                    <filter name="group_by_participant"
                            string="Participant"
                            context="{'group_by': 'participant_id'}"/>

                    <filter name="group_by_registration_date"
                            string="Registration Date"
                            context="{'group_by': 'registration_date'}"/>

                </group>

            </search>

        </field>
    </record>


    <!-- Registration Action -->
    <record id="action_event_registration" model="ir.actions.act_window">
        <field name="name">Registrations</field>
        <field name="res_model">event.registration</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_event_registration_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first registration!
            </p>
        </field>
    </record>


    <!-- Registration Menu -->
    <menuitem id="menu_event_registration"
              name="Registrations"
              parent="menu_event_management_root"
              action="action_event_registration"
              sequence="20"/>

</odoo>